# Cloudflare Workers AI needs no key when the AI binding is enabled.
CLOUDFLARE_AI_MODEL="@cf/google/gemma-4-26b-a4b-it"

# Optional fallback. The key is read only by the server.
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-2.5-flash"
